query Me {
  me {
    username
    roles
    borrowedRecords {
      bookId
      borrowedAt
    }
  }
}

mutation Register($input: RegisterInput!) {
  register(input: $input) {
    username
    roles
    borrowedRecords {
      bookId
      borrowedAt
    }
  }
}

